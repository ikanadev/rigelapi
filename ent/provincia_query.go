// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/vmkevv/rigelapi/ent/dpto"
	"github.com/vmkevv/rigelapi/ent/municipio"
	"github.com/vmkevv/rigelapi/ent/predicate"
	"github.com/vmkevv/rigelapi/ent/provincia"
)

// ProvinciaQuery is the builder for querying Provincia entities.
type ProvinciaQuery struct {
	config
	limit            *int
	offset           *int
	unique           *bool
	order            []OrderFunc
	fields           []string
	predicates       []predicate.Provincia
	withMunicipios   *MunicipioQuery
	withDepartamento *DptoQuery
	withFKs          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProvinciaQuery builder.
func (pq *ProvinciaQuery) Where(ps ...predicate.Provincia) *ProvinciaQuery {
	pq.predicates = append(pq.predicates, ps...)
	return pq
}

// Limit adds a limit step to the query.
func (pq *ProvinciaQuery) Limit(limit int) *ProvinciaQuery {
	pq.limit = &limit
	return pq
}

// Offset adds an offset step to the query.
func (pq *ProvinciaQuery) Offset(offset int) *ProvinciaQuery {
	pq.offset = &offset
	return pq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pq *ProvinciaQuery) Unique(unique bool) *ProvinciaQuery {
	pq.unique = &unique
	return pq
}

// Order adds an order step to the query.
func (pq *ProvinciaQuery) Order(o ...OrderFunc) *ProvinciaQuery {
	pq.order = append(pq.order, o...)
	return pq
}

// QueryMunicipios chains the current query on the "municipios" edge.
func (pq *ProvinciaQuery) QueryMunicipios() *MunicipioQuery {
	query := &MunicipioQuery{config: pq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(provincia.Table, provincia.FieldID, selector),
			sqlgraph.To(municipio.Table, municipio.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, provincia.MunicipiosTable, provincia.MunicipiosColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDepartamento chains the current query on the "departamento" edge.
func (pq *ProvinciaQuery) QueryDepartamento() *DptoQuery {
	query := &DptoQuery{config: pq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(provincia.Table, provincia.FieldID, selector),
			sqlgraph.To(dpto.Table, dpto.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, provincia.DepartamentoTable, provincia.DepartamentoColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Provincia entity from the query.
// Returns a *NotFoundError when no Provincia was found.
func (pq *ProvinciaQuery) First(ctx context.Context) (*Provincia, error) {
	nodes, err := pq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{provincia.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pq *ProvinciaQuery) FirstX(ctx context.Context) *Provincia {
	node, err := pq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Provincia ID from the query.
// Returns a *NotFoundError when no Provincia ID was found.
func (pq *ProvinciaQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = pq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{provincia.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pq *ProvinciaQuery) FirstIDX(ctx context.Context) string {
	id, err := pq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Provincia entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Provincia entity is found.
// Returns a *NotFoundError when no Provincia entities are found.
func (pq *ProvinciaQuery) Only(ctx context.Context) (*Provincia, error) {
	nodes, err := pq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{provincia.Label}
	default:
		return nil, &NotSingularError{provincia.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pq *ProvinciaQuery) OnlyX(ctx context.Context) *Provincia {
	node, err := pq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Provincia ID in the query.
// Returns a *NotSingularError when more than one Provincia ID is found.
// Returns a *NotFoundError when no entities are found.
func (pq *ProvinciaQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = pq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{provincia.Label}
	default:
		err = &NotSingularError{provincia.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pq *ProvinciaQuery) OnlyIDX(ctx context.Context) string {
	id, err := pq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProvinciaSlice.
func (pq *ProvinciaQuery) All(ctx context.Context) ([]*Provincia, error) {
	if err := pq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return pq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (pq *ProvinciaQuery) AllX(ctx context.Context) []*Provincia {
	nodes, err := pq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Provincia IDs.
func (pq *ProvinciaQuery) IDs(ctx context.Context) ([]string, error) {
	var ids []string
	if err := pq.Select(provincia.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pq *ProvinciaQuery) IDsX(ctx context.Context) []string {
	ids, err := pq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pq *ProvinciaQuery) Count(ctx context.Context) (int, error) {
	if err := pq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return pq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (pq *ProvinciaQuery) CountX(ctx context.Context) int {
	count, err := pq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pq *ProvinciaQuery) Exist(ctx context.Context) (bool, error) {
	if err := pq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return pq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (pq *ProvinciaQuery) ExistX(ctx context.Context) bool {
	exist, err := pq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProvinciaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pq *ProvinciaQuery) Clone() *ProvinciaQuery {
	if pq == nil {
		return nil
	}
	return &ProvinciaQuery{
		config:           pq.config,
		limit:            pq.limit,
		offset:           pq.offset,
		order:            append([]OrderFunc{}, pq.order...),
		predicates:       append([]predicate.Provincia{}, pq.predicates...),
		withMunicipios:   pq.withMunicipios.Clone(),
		withDepartamento: pq.withDepartamento.Clone(),
		// clone intermediate query.
		sql:    pq.sql.Clone(),
		path:   pq.path,
		unique: pq.unique,
	}
}

// WithMunicipios tells the query-builder to eager-load the nodes that are connected to
// the "municipios" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *ProvinciaQuery) WithMunicipios(opts ...func(*MunicipioQuery)) *ProvinciaQuery {
	query := &MunicipioQuery{config: pq.config}
	for _, opt := range opts {
		opt(query)
	}
	pq.withMunicipios = query
	return pq
}

// WithDepartamento tells the query-builder to eager-load the nodes that are connected to
// the "departamento" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *ProvinciaQuery) WithDepartamento(opts ...func(*DptoQuery)) *ProvinciaQuery {
	query := &DptoQuery{config: pq.config}
	for _, opt := range opts {
		opt(query)
	}
	pq.withDepartamento = query
	return pq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Provincia.Query().
//		GroupBy(provincia.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pq *ProvinciaQuery) GroupBy(field string, fields ...string) *ProvinciaGroupBy {
	grbuild := &ProvinciaGroupBy{config: pq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return pq.sqlQuery(ctx), nil
	}
	grbuild.label = provincia.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.Provincia.Query().
//		Select(provincia.FieldName).
//		Scan(ctx, &v)
func (pq *ProvinciaQuery) Select(fields ...string) *ProvinciaSelect {
	pq.fields = append(pq.fields, fields...)
	selbuild := &ProvinciaSelect{ProvinciaQuery: pq}
	selbuild.label = provincia.Label
	selbuild.flds, selbuild.scan = &pq.fields, selbuild.Scan
	return selbuild
}

func (pq *ProvinciaQuery) prepareQuery(ctx context.Context) error {
	for _, f := range pq.fields {
		if !provincia.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pq.path != nil {
		prev, err := pq.path(ctx)
		if err != nil {
			return err
		}
		pq.sql = prev
	}
	return nil
}

func (pq *ProvinciaQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Provincia, error) {
	var (
		nodes       = []*Provincia{}
		withFKs     = pq.withFKs
		_spec       = pq.querySpec()
		loadedTypes = [2]bool{
			pq.withMunicipios != nil,
			pq.withDepartamento != nil,
		}
	)
	if pq.withDepartamento != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, provincia.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*Provincia).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &Provincia{config: pq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pq.withMunicipios; query != nil {
		if err := pq.loadMunicipios(ctx, query, nodes,
			func(n *Provincia) { n.Edges.Municipios = []*Municipio{} },
			func(n *Provincia, e *Municipio) { n.Edges.Municipios = append(n.Edges.Municipios, e) }); err != nil {
			return nil, err
		}
	}
	if query := pq.withDepartamento; query != nil {
		if err := pq.loadDepartamento(ctx, query, nodes, nil,
			func(n *Provincia, e *Dpto) { n.Edges.Departamento = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pq *ProvinciaQuery) loadMunicipios(ctx context.Context, query *MunicipioQuery, nodes []*Provincia, init func(*Provincia), assign func(*Provincia, *Municipio)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Provincia)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Municipio(func(s *sql.Selector) {
		s.Where(sql.InValues(provincia.MunicipiosColumn, fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.provincia_municipios
		if fk == nil {
			return fmt.Errorf(`foreign-key "provincia_municipios" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "provincia_municipios" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (pq *ProvinciaQuery) loadDepartamento(ctx context.Context, query *DptoQuery, nodes []*Provincia, init func(*Provincia), assign func(*Provincia, *Dpto)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*Provincia)
	for i := range nodes {
		if nodes[i].dpto_provincias == nil {
			continue
		}
		fk := *nodes[i].dpto_provincias
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	query.Where(dpto.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "dpto_provincias" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pq *ProvinciaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pq.querySpec()
	_spec.Node.Columns = pq.fields
	if len(pq.fields) > 0 {
		_spec.Unique = pq.unique != nil && *pq.unique
	}
	return sqlgraph.CountNodes(ctx, pq.driver, _spec)
}

func (pq *ProvinciaQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := pq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (pq *ProvinciaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   provincia.Table,
			Columns: provincia.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeString,
				Column: provincia.FieldID,
			},
		},
		From:   pq.sql,
		Unique: true,
	}
	if unique := pq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := pq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, provincia.FieldID)
		for i := range fields {
			if fields[i] != provincia.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pq *ProvinciaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pq.driver.Dialect())
	t1 := builder.Table(provincia.Table)
	columns := pq.fields
	if len(columns) == 0 {
		columns = provincia.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pq.sql != nil {
		selector = pq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pq.unique != nil && *pq.unique {
		selector.Distinct()
	}
	for _, p := range pq.predicates {
		p(selector)
	}
	for _, p := range pq.order {
		p(selector)
	}
	if offset := pq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProvinciaGroupBy is the group-by builder for Provincia entities.
type ProvinciaGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pgb *ProvinciaGroupBy) Aggregate(fns ...AggregateFunc) *ProvinciaGroupBy {
	pgb.fns = append(pgb.fns, fns...)
	return pgb
}

// Scan applies the group-by query and scans the result into the given value.
func (pgb *ProvinciaGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := pgb.path(ctx)
	if err != nil {
		return err
	}
	pgb.sql = query
	return pgb.sqlScan(ctx, v)
}

func (pgb *ProvinciaGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range pgb.fields {
		if !provincia.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := pgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (pgb *ProvinciaGroupBy) sqlQuery() *sql.Selector {
	selector := pgb.sql.Select()
	aggregation := make([]string, 0, len(pgb.fns))
	for _, fn := range pgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(pgb.fields)+len(pgb.fns))
		for _, f := range pgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(pgb.fields...)...)
}

// ProvinciaSelect is the builder for selecting fields of Provincia entities.
type ProvinciaSelect struct {
	*ProvinciaQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (ps *ProvinciaSelect) Scan(ctx context.Context, v interface{}) error {
	if err := ps.prepareQuery(ctx); err != nil {
		return err
	}
	ps.sql = ps.ProvinciaQuery.sqlQuery(ctx)
	return ps.sqlScan(ctx, v)
}

func (ps *ProvinciaSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := ps.sql.Query()
	if err := ps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
