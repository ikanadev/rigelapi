// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/vmkevv/rigelapi/ent/attendancesync"
	"github.com/vmkevv/rigelapi/ent/teacher"
)

// AttendanceSync is the model entity for the AttendanceSync schema.
type AttendanceSync struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// LastSyncID holds the value of the "last_sync_id" field.
	LastSyncID string `json:"last_sync_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AttendanceSyncQuery when eager-loading is set.
	Edges                    AttendanceSyncEdges `json:"edges"`
	teacher_attendance_syncs *string
}

// AttendanceSyncEdges holds the relations/edges for other nodes in the graph.
type AttendanceSyncEdges struct {
	// Teacher holds the value of the teacher edge.
	Teacher *Teacher `json:"teacher,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TeacherOrErr returns the Teacher value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AttendanceSyncEdges) TeacherOrErr() (*Teacher, error) {
	if e.loadedTypes[0] {
		if e.Teacher == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: teacher.Label}
		}
		return e.Teacher, nil
	}
	return nil, &NotLoadedError{edge: "teacher"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AttendanceSync) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case attendancesync.FieldID, attendancesync.FieldLastSyncID:
			values[i] = new(sql.NullString)
		case attendancesync.ForeignKeys[0]: // teacher_attendance_syncs
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type AttendanceSync", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AttendanceSync fields.
func (as *AttendanceSync) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case attendancesync.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				as.ID = value.String
			}
		case attendancesync.FieldLastSyncID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field last_sync_id", values[i])
			} else if value.Valid {
				as.LastSyncID = value.String
			}
		case attendancesync.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field teacher_attendance_syncs", values[i])
			} else if value.Valid {
				as.teacher_attendance_syncs = new(string)
				*as.teacher_attendance_syncs = value.String
			}
		}
	}
	return nil
}

// QueryTeacher queries the "teacher" edge of the AttendanceSync entity.
func (as *AttendanceSync) QueryTeacher() *TeacherQuery {
	return (&AttendanceSyncClient{config: as.config}).QueryTeacher(as)
}

// Update returns a builder for updating this AttendanceSync.
// Note that you need to call AttendanceSync.Unwrap() before calling this method if this AttendanceSync
// was returned from a transaction, and the transaction was committed or rolled back.
func (as *AttendanceSync) Update() *AttendanceSyncUpdateOne {
	return (&AttendanceSyncClient{config: as.config}).UpdateOne(as)
}

// Unwrap unwraps the AttendanceSync entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (as *AttendanceSync) Unwrap() *AttendanceSync {
	_tx, ok := as.config.driver.(*txDriver)
	if !ok {
		panic("ent: AttendanceSync is not a transactional entity")
	}
	as.config.driver = _tx.drv
	return as
}

// String implements the fmt.Stringer.
func (as *AttendanceSync) String() string {
	var builder strings.Builder
	builder.WriteString("AttendanceSync(")
	builder.WriteString(fmt.Sprintf("id=%v, ", as.ID))
	builder.WriteString("last_sync_id=")
	builder.WriteString(as.LastSyncID)
	builder.WriteByte(')')
	return builder.String()
}

// AttendanceSyncs is a parsable slice of AttendanceSync.
type AttendanceSyncs []*AttendanceSync

func (as AttendanceSyncs) config(cfg config) {
	for _i := range as {
		as[_i].config = cfg
	}
}
