// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/vmkevv/rigelapi/ent/scoresync"
	"github.com/vmkevv/rigelapi/ent/teacher"
)

// ScoreSync is the model entity for the ScoreSync schema.
type ScoreSync struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// LastSyncID holds the value of the "last_sync_id" field.
	LastSyncID string `json:"last_sync_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ScoreSyncQuery when eager-loading is set.
	Edges               ScoreSyncEdges `json:"edges"`
	teacher_score_syncs *string
}

// ScoreSyncEdges holds the relations/edges for other nodes in the graph.
type ScoreSyncEdges struct {
	// Teacher holds the value of the teacher edge.
	Teacher *Teacher `json:"teacher,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TeacherOrErr returns the Teacher value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ScoreSyncEdges) TeacherOrErr() (*Teacher, error) {
	if e.loadedTypes[0] {
		if e.Teacher == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: teacher.Label}
		}
		return e.Teacher, nil
	}
	return nil, &NotLoadedError{edge: "teacher"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ScoreSync) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case scoresync.FieldID, scoresync.FieldLastSyncID:
			values[i] = new(sql.NullString)
		case scoresync.ForeignKeys[0]: // teacher_score_syncs
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type ScoreSync", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ScoreSync fields.
func (ss *ScoreSync) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case scoresync.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				ss.ID = value.String
			}
		case scoresync.FieldLastSyncID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field last_sync_id", values[i])
			} else if value.Valid {
				ss.LastSyncID = value.String
			}
		case scoresync.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field teacher_score_syncs", values[i])
			} else if value.Valid {
				ss.teacher_score_syncs = new(string)
				*ss.teacher_score_syncs = value.String
			}
		}
	}
	return nil
}

// QueryTeacher queries the "teacher" edge of the ScoreSync entity.
func (ss *ScoreSync) QueryTeacher() *TeacherQuery {
	return (&ScoreSyncClient{config: ss.config}).QueryTeacher(ss)
}

// Update returns a builder for updating this ScoreSync.
// Note that you need to call ScoreSync.Unwrap() before calling this method if this ScoreSync
// was returned from a transaction, and the transaction was committed or rolled back.
func (ss *ScoreSync) Update() *ScoreSyncUpdateOne {
	return (&ScoreSyncClient{config: ss.config}).UpdateOne(ss)
}

// Unwrap unwraps the ScoreSync entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ss *ScoreSync) Unwrap() *ScoreSync {
	_tx, ok := ss.config.driver.(*txDriver)
	if !ok {
		panic("ent: ScoreSync is not a transactional entity")
	}
	ss.config.driver = _tx.drv
	return ss
}

// String implements the fmt.Stringer.
func (ss *ScoreSync) String() string {
	var builder strings.Builder
	builder.WriteString("ScoreSync(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ss.ID))
	builder.WriteString("last_sync_id=")
	builder.WriteString(ss.LastSyncID)
	builder.WriteByte(')')
	return builder.String()
}

// ScoreSyncs is a parsable slice of ScoreSync.
type ScoreSyncs []*ScoreSync

func (ss ScoreSyncs) config(cfg config) {
	for _i := range ss {
		ss[_i].config = cfg
	}
}
