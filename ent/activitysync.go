// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/vmkevv/rigelapi/ent/activitysync"
	"github.com/vmkevv/rigelapi/ent/teacher"
)

// ActivitySync is the model entity for the ActivitySync schema.
type ActivitySync struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// LastSyncID holds the value of the "last_sync_id" field.
	LastSyncID string `json:"last_sync_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ActivitySyncQuery when eager-loading is set.
	Edges                  ActivitySyncEdges `json:"edges"`
	teacher_activity_syncs *string
}

// ActivitySyncEdges holds the relations/edges for other nodes in the graph.
type ActivitySyncEdges struct {
	// Teacher holds the value of the teacher edge.
	Teacher *Teacher `json:"teacher,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TeacherOrErr returns the Teacher value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ActivitySyncEdges) TeacherOrErr() (*Teacher, error) {
	if e.loadedTypes[0] {
		if e.Teacher == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: teacher.Label}
		}
		return e.Teacher, nil
	}
	return nil, &NotLoadedError{edge: "teacher"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ActivitySync) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case activitysync.FieldID, activitysync.FieldLastSyncID:
			values[i] = new(sql.NullString)
		case activitysync.ForeignKeys[0]: // teacher_activity_syncs
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type ActivitySync", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ActivitySync fields.
func (as *ActivitySync) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case activitysync.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				as.ID = value.String
			}
		case activitysync.FieldLastSyncID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field last_sync_id", values[i])
			} else if value.Valid {
				as.LastSyncID = value.String
			}
		case activitysync.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field teacher_activity_syncs", values[i])
			} else if value.Valid {
				as.teacher_activity_syncs = new(string)
				*as.teacher_activity_syncs = value.String
			}
		}
	}
	return nil
}

// QueryTeacher queries the "teacher" edge of the ActivitySync entity.
func (as *ActivitySync) QueryTeacher() *TeacherQuery {
	return (&ActivitySyncClient{config: as.config}).QueryTeacher(as)
}

// Update returns a builder for updating this ActivitySync.
// Note that you need to call ActivitySync.Unwrap() before calling this method if this ActivitySync
// was returned from a transaction, and the transaction was committed or rolled back.
func (as *ActivitySync) Update() *ActivitySyncUpdateOne {
	return (&ActivitySyncClient{config: as.config}).UpdateOne(as)
}

// Unwrap unwraps the ActivitySync entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (as *ActivitySync) Unwrap() *ActivitySync {
	_tx, ok := as.config.driver.(*txDriver)
	if !ok {
		panic("ent: ActivitySync is not a transactional entity")
	}
	as.config.driver = _tx.drv
	return as
}

// String implements the fmt.Stringer.
func (as *ActivitySync) String() string {
	var builder strings.Builder
	builder.WriteString("ActivitySync(")
	builder.WriteString(fmt.Sprintf("id=%v, ", as.ID))
	builder.WriteString("last_sync_id=")
	builder.WriteString(as.LastSyncID)
	builder.WriteByte(')')
	return builder.String()
}

// ActivitySyncs is a parsable slice of ActivitySync.
type ActivitySyncs []*ActivitySync

func (as ActivitySyncs) config(cfg config) {
	for _i := range as {
		as[_i].config = cfg
	}
}
